name: CodeQL (disabled)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  schedule:
    - cron: '0 3 * * 0'

jobs:
  analyse:
    # Disabled because public CodeQL does not (yet) include GA support for PHP in this repository.
    # We already run static analysis via PHPStan/Larastan in ci.yml. Re‑enable once PHP becomes
    # generally available (or this repo is enrolled in the beta) by removing the `if: false` and
    # restoring the language matrix.
    if: false
    name: Analyse (temporarily disabled)
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: |
          echo "CodeQL disabled: relying on PHPStan & Mutation testing instead."
